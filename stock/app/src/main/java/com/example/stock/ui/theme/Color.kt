package com.example.stock.ui.theme

import androidx.compose.ui.graphics.Color

val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

val StockBackground = Color(0xFFFFFFFF)              // 순수한 흰색 배경
val StockForeground = Color(0xFF09090B)              // 텍스트 및 전경 요소 (거의 검정)
val StockCard = Color(0xFFFAFAFA)                     // 카드 배경 (배경과 미세한 차이)
val StockCardForeground = Color(0xFF09090B)          // 카드 위 텍스트
val StockMuted = Color(0xFF71717A)                    // 보조 텍스트 (예: placeholder)
val StockMutedForeground = Color(0xFF71717A)        // 보조 텍스트 색상
val StockBorder = Color(0xFFE4E4E7)                  // 테두리선
val StockInput = Color(0xFFFFFFFF)                    // 입력 필드 배경

// Accent (Primary) Colors
val StockPrimary = Color(0xFF09090B)                  // 주요 액션 (버튼 등)
val StockPrimaryForeground = Color(0xFFFAFAFA)        // Primary 색상 위의 텍스트

// --- 추가된 Secondary Colors ---
val StockSecondary = Color(0xFFDD2A3B)                // 새로운 보조 액션 색상 (#dd2a3b)
val StockSecondaryForeground = Color(0xFFFAFAFA)      // Secondary 색상 위의 텍스트 (가독성을 위해 PrimaryForeground 재사용)